//normally this page.js file would correspond to url 'localhost:3000/_lib'
//but it doesnt because of underscore before file name :)

const PrivateRoute = () => {
    return(
        <h1>You cannot view this in the browser</h1>
    )
}

export default PrivateRoute;